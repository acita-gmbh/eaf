# pnpm configuration for EAF monorepo
#
# Public hoist pattern - hoist common dependencies to root node_modules
# This ensures workspace libraries can find peer dependencies
public-hoist-pattern[]=*@mui/*
public-hoist-pattern[]=react-admin
public-hoist-pattern[]=react
public-hoist-pattern[]=react-dom

# Enable shamefully-hoist for better compatibility (if needed)
shamefully-hoist=true
