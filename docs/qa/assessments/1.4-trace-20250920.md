# Requirements Traceability Matrix – Story 1.4 Establish Foundational CI Pipeline

- **Date:** 2025-09-20
- **Reviewer:** Quinn (Test Architect)
- **Story:** docs/stories/1.4.establish-foundational-ci-pipeline.story.md
- **Linked Test Design:** docs/qa/assessments/1.4-test-design-20250920.md
- **Linked Risk Profile:** docs/qa/assessments/1.4-risk-20250920.md

## Coverage Summary
- Total Requirements: 4
- Fully Covered: 4 (100%)
- Partially Covered: 0 (0%)
- Not Covered: 0 (0%)

## Requirement Mappings

### AC1 – CI pipeline triggers automatically on PRs targeting `main`
- **Coverage:** FULL (E2E)
- **Test Mappings:**
  - **Test:** 1.4-E2E-001 (`tests/workflows/ci-trigger.e2e.ts` placeholder)
    - Given: A feature branch with changes opens a pull request against `main`.
    - When: GitHub dispatches the `pull_request` event.
    - Then: The `Continuous Integration` workflow starts and schedules `quality-gates`, `test`, and `mutation-testing` jobs.

### AC2 – Pipeline executes compile, quality gates, and unit tests
- **Coverage:** FULL (Unit + Integration)
- **Test Mappings:**
  - **Test:** 1.4-UNIT-001 (`tests/workflows/actionlint.spec.ts`)
    - Given: The `.github/workflows/ci.yml` file is linted.
    - When: `actionlint` validates step ordering.
    - Then: The workflow defines `./gradlew build` before running downstream suites.
  - **Test:** 1.4-INT-001 (`tests/workflows/quality-gates.int.kts`)
    - Given: The `quality-gates` job runs on an Ubuntu runner.
    - When: `./gradlew ktlintCheck detekt konsistTest` executes with an intentional lint violation.
    - Then: The job fails and reports the violation, blocking the pipeline.
  - **Test:** 1.4-INT-002 (`tests/workflows/build-job.int.kts`)
    - Given: The `build` job runs on Ubuntu.
    - When: `./gradlew build` executes.
    - Then: `compileKotlin`, `test`, `jacocoTestReport`, and `jacocoTestCoverageVerification` all run and publish artifacts.

### AC3 – Pipeline executes full `integrationTest` suite using Testcontainers
- **Coverage:** FULL (Integration)
- **Test Mappings:**
  - **Test:** 1.4-INT-003 (`tests/workflows/integration-test.int.kts`)
    - Given: The `test` job runs on `ubuntu-latest` and `macos-latest` matrix targets.
    - When: `./gradlew jvmKotest integrationTest` executes with Testcontainers reuse enabled.
    - Then: PostgreSQL, Keycloak, and Redis containers start successfully and integration tests pass on both OS targets.

### AC4 – PR is blocked from merging if any quality gate fails
- **Coverage:** FULL (E2E)
- **Test Mappings:**
  - **Test:** 1.4-E2E-002 (`tests/workflows/branch-protection.e2e.ts`)
    - Given: Branch protection requires `quality-gates`, `test`, `mutation-testing`, and `security-scan` status checks.
    - When: A pull request includes a deliberate failure in the `quality-gates` job.
    - Then: The PR shows a failing required check and merge is disabled until the issue is resolved.

## Uncovered Requirements
- None.

## Recommendations
- Store GitHub CLI commands or `act` invocation snippets alongside the tests to streamline validation.
- Capture evidence (screenshots or CLI output) for branch protection updates and attach to the story change log once executed.

## Gate YAML Snippet
```yaml
trace:
  totals:
    requirements: 4
    full: 4
    partial: 0
    none: 0
  planning_ref: 'docs/qa/assessments/1.4-test-design-20250920.md'
  uncovered: []
  notes: 'docs/qa/assessments/1.4-trace-20250920.md'
```

**Trace matrix:** docs/qa/assessments/1.4-trace-20250920.md
