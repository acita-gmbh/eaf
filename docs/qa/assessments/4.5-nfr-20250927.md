# NFR Assessment: Story 4.5

**Story**: Migrate Widget Domain Code from Framework to Product Module
**Date**: 2025-09-27
**Reviewer**: Quinn (Test Architect)
**Analysis Method**: Ollama (gpt-oss:20b with thinking)

## Summary

| NFR Category | Status | Quality Score Impact |
|--------------|--------|---------------------|
| **Security** | ⚠️ CONCERNS | -10 points |
| **Performance** | ✅ PASS | 0 points |
| **Reliability** | ⚠️ CONCERNS | -10 points |
| **Maintainability** | ✅ PASS | 0 points |

**Overall NFR Score**: 80/100

---

## Security Assessment

### Status: ⚠️ CONCERNS

### Requirements
1. No secrets or credentials may be introduced or exposed by refactoring
2. Migration must NOT introduce new security-relevant dependencies (external libraries with CVE history)
3. All references to framework-level security artifacts (`@PreAuthorize`, security filters) must be transferred to product module while preserving policy
4. No exposure of widget domain data during migration

### Evidence Required
- Static-analysis scan for hard-coded secrets (GitLeaks, TruffleHog)
- Dependency-scan (OWASP Dependency-Check) showing zero vulnerable 3rd-party libs
- Security test audit log capturing all `@PreAuthorize`/`@Secured` directives
- Confirmation that security policies still apply after migration

### Assessment
The story plan requires:
- ✅ "All migrated tests must pass"
- ✅ "No framework/widget references remain"
- ✅ "Documentation updated"

However:
- ❌ **No explicit secret-scan step** in story
- ❌ **No dependency vulnerability scan** mentioned
- ❌ **No audit trail of security-policy changes**

The existing test-suite likely doesn't catch accidental secret exposure, nor validate that no new security-critical dependencies were added.

**Conclusion**: Requirement is PARTIALLY satisfied - tests exist, but evidence base is incomplete.

### Gaps Identified
1. Missing explicit secret/dependency scan step
2. No definition of "security-critical" dependencies in migration checklist
3. No audit trail of security-policy changes (e.g., `@PreAuthorize` migration)

### Recommendations

**IMMEDIATE (Must Fix Before Implementation)**:

1. **Add Secret Scanning Validation** (HIGH PRIORITY):
   ```
   NEW Subtask 9.4: Run secret scanning on all migrated files
   - Command: detect-secrets scan --all-files framework/widget/ products/widget-demo/
   - Success Criteria: Zero secrets detected
   - Add to Task 9 (before module deletion)
   ```

2. **Add Dependency Vulnerability Scan**:
   ```
   NEW Subtask 11.5: Run OWASP Dependency Check on products/widget-demo
   - Command: ./gradlew :products:widget-demo:dependencyCheckAnalyze
   - Success Criteria: Zero HIGH/CRITICAL vulnerabilities
   - Add to Task 11 (validation phase)
   ```

3. **Document Security Annotations**:
   ```
   Update Subtask 13.3: Include security annotation audit in migration note
   - Document all @PreAuthorize, @Secured annotations migrated
   - Verify no security policies lost during migration
   ```

**FUTURE (Post-Migration)**:
- Add automated secret scanning to CI/CD pipeline
- Implement dependency scanning in pre-commit hooks
- Create security policy migration checklist template

---

## Performance Assessment

### Status: ✅ PASS

### Requirements
1. Incremental build time must stay <10 seconds
2. Full build (clean) time must stay <2 minutes
3. Build time variance must stay within ±5%
4. Startup time must stay within ±10% of baseline
5. No performance degradation in widget functionality

### Evidence Required
- CI build logs showing incremental and full build durations
- Benchmark report comparing baseline vs migrated build times
- Application startup metrics (logs or profiling)
- Performance validation commands output

### Assessment
Story plan explicitly specifies:
- ✅ "Build within 5% variance, startup within 10% variance" (Dev Notes line 159-161)
- ✅ Performance Validation section with specific thresholds
- ✅ Baseline capture in Task 1.3
- ✅ Post-migration validation in Task 11.2

Migration is limited to domain package move (not adding significant dependencies), so performance degradation is highly unlikely to exceed thresholds.

### Gaps Identified
**NONE** - Performance validation is comprehensive and well-specified.

### Recommendations
- ✅ Keep existing performance-monitoring scripts
- ✅ Generate performance diff report attached to PR
- ✅ Verify migration repo's build.gradle has no additional heavy plugins

**Additional Insight**: Story already includes optional mutation testing (Subtask 12.3) which can impact build time. Consider tracking this separately from core build metrics.

---

## Reliability Assessment

### Status: ⚠️ CONCERNS

### Requirements
1. Refactor must be transactional or rollback-capable
2. Migration must preserve behavior (all integration tests pass against live instance)
3. New product module must not introduce hidden race-conditions (duplicate bean definitions)
4. Graceful degradation if migration encounters unexpected issues
5. Clear recovery path if migration fails

### Evidence Required
- CI results showing all integration tests passing on fresh clone
- Failure-mode simulation validating rollback capability
- Manual audit of Spring Modulith boundaries (no duplicate `@Component` scans)
- Documented rollback procedure with validation steps

### Assessment
Story plan includes:
- ✅ "All migrated tests must pass" (multiple tasks)
- ✅ "Application starts successfully" (Task 11.2)
- ✅ Rollback Strategy documented (Dev Notes lines 222-227)

However:
- ❌ **No feature-flag or toggle mechanism** for safe rollback
- ❌ **No explicit duplicate bean detection** validation
- ⚠️ **Rollback strategy is manual, not automated**

Without automated rollback guard, unexpected bug in new module could impact production availability.

### Gaps Identified
1. No explicit rollback/feature-toggle procedure automated
2. No explicit duplicate bean detection test
3. No hot-swap test or migration validation in clean environment

### Recommendations

**IMMEDIATE (Improve Reliability)**:

1. **Add Feature Flag Support** (OPTIONAL but RECOMMENDED):
   ```
   Consider: Add widget.enabled flag to application.yml
   - Allows disabling widget-demo module if regression occurs
   - Provides safe rollback without code changes
   - Low effort, high reliability gain
   ```

2. **Add Bean Conflict Detection**:
   ```
   NEW Subtask 11.6: Spring bean conflict scan
   - Command: Custom script using ApplicationContext.getBeansWithAnnotation
   - Validation: No duplicate @Component, @Service, @EventHandler beans
   - Success: Each bean has single definition
   - Add to Task 11 (validation phase)
   ```

3. **Enhance Rollback Validation**:
   ```
   Update Subtask 1.1: Add rollback dry-run
   - After baseline capture, create backup branch
   - Document rollback validation steps
   - Test rollback procedure in separate branch before migration
   ```

**FUTURE**:
- Implement automated rollback tests in CI
- Create migration validation framework for future refactorings
- Add health-check validation for widget-demo endpoints

---

## Maintainability Assessment

### Status: ✅ PASS

### Requirements
1. Source-level test coverage must remain ≥85% (lines)
2. Mutation coverage ≥80%
3. Zero violations of detekt & ktlint
4. Architecture boundaries must hold (product module must not leak into framework package)
5. Documentation must be updated to reflect new package path and public APIs

### Evidence Required
- JaCoCo code-coverage reports showing ≥85% line coverage, ≥80% mutation coverage pre/post-migration
- detekt & ktlint reports (must be empty)
- Architecture tool results (Konsist) confirming no cross-module dependencies
- Updated module documentation verified via doc-scan script

### Assessment
Story plan explicitly includes:
- ✅ Test coverage validation: Subtask 12.2 (≥85% line coverage)
- ✅ Mutation testing: Subtask 12.3 (≥80% mutation coverage, optional)
- ✅ Static analysis: Subtask 12.1 (detekt, ktlint zero violations)
- ✅ Architecture compliance: Subtask 11.4 (konsistTest validation)
- ✅ Documentation updates: Task 13 (all docs updated)

Migration of domain logic (business rules only) is unlikely to affect test coverage or linting; mainly shifts package ownership. The only risk is accidentally "leaking" reference back into framework layer, but that would be caught by architecture compliance check (konsistTest).

### Gaps Identified
1. ⚠️ **Mutation coverage trigger not mandatory** - Currently optional in Subtask 12.3
2. ℹ️ **detekt/ktlint not specified to run on new module only** - Could run on all modules (less efficient)

### Recommendations

**SHOULD IMPLEMENT**:

1. **Make Mutation Testing Mandatory** (P1):
   ```
   Update Subtask 12.3: Remove "OPTIONAL" designation
   - Rationale: Migration is refactoring (behavior must be preserved)
   - Mutation testing best validates behavioral equivalence
   - Estimated time: +10-15 minutes to build time
   ```

2. **Optimize Static Analysis Execution**:
   ```
   Update Subtask 12.1: Run static analysis on changed modules only
   - Command: ./gradlew :products:widget-demo:detekt :products:widget-demo:ktlintCheck
   - Benefit: Faster feedback, focused results
   ```

3. **Add Architecture Validation Baseline**:
   ```
   NEW Subtask 1.4: Capture konsistTest baseline before migration
   - Establishes architectural compliance baseline
   - Enables precise before/after comparison
   - Detects any unintended boundary violations
   ```

**NICE-TO-HAVE**:
- Add mutation coverage trending (track over time)
- Generate architecture diff report (Konsist before/after)
- Create maintainability scorecard for product modules

---

## Critical Issues

### Issue 1: Missing Secret Scanning (Security)
- **Risk**: Hard-coded credentials or API keys may be exposed during migration
- **Impact**: Audit compliance failure, potential security breach
- **Fix**: Add secret scanning to Task 9 (before module deletion)
- **Effort**: ~30 minutes
- **Priority**: HIGH

### Issue 2: No Feature Flag for Rollback (Reliability)
- **Risk**: If regression occurs post-merge, rollback requires code changes
- **Impact**: Extended downtime during incident response
- **Fix**: Add widget.enabled flag (optional but recommended)
- **Effort**: ~1 hour
- **Priority**: MEDIUM

## Quick Wins

### Win 1: Add Secret Scanning
- **Effort**: 30 minutes
- **Impact**: Closes security gap
- **Implementation**: Add single subtask with detect-secrets command

### Win 2: Make Mutation Testing Mandatory
- **Effort**: 0 minutes (just remove "OPTIONAL" designation)
- **Impact**: Improves behavioral validation confidence
- **Implementation**: Edit Subtask 12.3 description

### Win 3: Add Bean Conflict Detection
- **Effort**: 1 hour (custom script + subtask)
- **Impact**: Prevents duplicate bean issues
- **Implementation**: Add new subtask with reflection-based scan

---

## NFR Compliance Matrix

| NFR | Requirement | Story Coverage | Evidence Plan | Status |
|-----|-------------|----------------|---------------|--------|
| **Security - Authentication** | Preserve existing JWT validation | Tests migrated (Task 6-7) | Integration tests pass | ✅ ADDRESSED |
| **Security - Authorization** | Preserve tenant isolation | No code changes | Existing tests validate | ✅ ADDRESSED |
| **Security - Input Validation** | No new inputs introduced | Refactoring only | N/A | ✅ N/A |
| **Security - Secret Management** | No secrets exposed | ❌ Missing scan | **ADD scanning** | ⚠️ GAP |
| **Security - Dependency Vulnerabilities** | No new CVEs | ❌ Missing scan | **ADD OWASP check** | ⚠️ GAP |
| **Performance - Response Time** | Maintain baselines | Task 11.2 validates | Startup time check | ✅ ADDRESSED |
| **Performance - Throughput** | No degradation | N/A (refactoring) | N/A | ✅ N/A |
| **Performance - Resource Usage** | No increase | Build variance check | 5% threshold | ✅ ADDRESSED |
| **Reliability - Error Handling** | Preserve existing | Tests migrated | Integration tests | ✅ ADDRESSED |
| **Reliability - Recovery** | Rollback capability | Manual procedure | **ADD automation** | ⚠️ PARTIAL |
| **Reliability - Availability** | No downtime | Deployment strategy | **ADD feature flag** | ⚠️ PARTIAL |
| **Maintainability - Test Coverage** | ≥85% line, ≥80% mutation | Task 12.2-12.3 | JaCoCo + Pitest | ✅ ADDRESSED |
| **Maintainability - Code Quality** | Zero violations | Task 12.1 | detekt, ktlint | ✅ ADDRESSED |
| **Maintainability - Architecture** | Module boundaries | Task 11.4 | konsistTest | ✅ ADDRESSED |
| **Maintainability - Documentation** | Up-to-date docs | Task 13 | Docs updated | ✅ ADDRESSED |

---

## Detailed NFR Analysis

### Security NFRs

**Status**: ⚠️ CONCERNS

**Assessed Requirements**:
1. ✅ **No new authentication/authorization code** - Migration preserves existing security model
2. ✅ **Tenant isolation preserved** - TenantContext usage unchanged
3. ⚠️ **No secret exposure** - NOT VALIDATED (missing scan)
4. ⚠️ **No vulnerable dependencies** - NOT VALIDATED (missing OWASP check)
5. ✅ **Security testing migrated** - TenantBoundaryValidationIntegrationTest moves with code

**Evidence in Story**:
- Subtask 7.2: Migrates `TenantBoundaryValidationIntegrationTest.kt` (security validation)
- Dev Notes: Documents TenantContext usage (lines 191-194)

**Missing Evidence**:
- No secret scanning step
- No dependency vulnerability assessment

**Recommendation**: **CONCERNS** status due to missing validation steps. Add secret scanning and OWASP dependency check to achieve PASS.

---

### Performance NFRs

**Status**: ✅ PASS

**Assessed Requirements**:
1. ✅ **Incremental build time <10 seconds** - Story includes baseline capture and validation
2. ✅ **Full build time <2 minutes** - Clean build validated in Task 11.1
3. ✅ **Build variance within ±5%** - Explicitly stated in Performance Validation (line 159)
4. ✅ **Startup variance within ±10%** - Explicitly stated in Performance Validation (line 161)
5. ✅ **No widget functionality degradation** - Integration tests validate functional equivalence

**Evidence in Story**:
- Subtask 1.3: Baseline capture before migration
- Subtask 11.1: Full build validation after migration
- Subtask 11.2: Application startup validation
- Dev Notes lines 158-161: Performance Validation thresholds

**Assessment**: Performance validation is comprehensive and well-specified. Migration is limited to package move (no algorithmic changes), so degradation beyond thresholds is highly unlikely.

**Conclusion**: ✅ PASS - All performance requirements adequately addressed.

---

### Reliability NFRs

**Status**: ⚠️ CONCERNS

**Assessed Requirements**:
1. ✅ **Behavior preservation** - All integration tests must pass (Task 7, AC5)
2. ✅ **Application startup reliability** - Validated in Task 11.2
3. ⚠️ **Rollback capability** - Manual procedure documented but not automated
4. ⚠️ **No duplicate beans** - Not explicitly validated
5. ✅ **Error handling preserved** - Tests migrated unchanged

**Evidence in Story**:
- Subtask 11.2: Application startup validation
- Task 7: All integration tests migrated and must pass
- Dev Notes lines 222-227: Rollback Strategy documented

**Missing Evidence**:
- No feature-flag or automated rollback mechanism
- No duplicate bean detection test
- Rollback strategy is manual, not automated

**Assessment**: Story includes rollback procedure, but it's reactive (use after failure) rather than proactive (built-in safety). Without automated rollback guard, unexpected bug could impact production.

**Conclusion**: ⚠️ CONCERNS due to missing automated reliability safeguards.

### Gaps Identified
1. No feature-flag/toggle procedure for runtime rollback
2. No explicit duplicate bean detection
3. No automated rollback validation test

### Recommendations

**SHOULD IMPLEMENT** (Improves Reliability to PASS):

1. **Add Bean Conflict Detection** (MEDIUM PRIORITY):
   ```
   NEW Subtask 11.6: Spring bean conflict scan
   - Approach: Use ApplicationContext.getBeansWithAnnotation to detect duplicates
   - Validation: No duplicate @Component, @Service, @EventHandler beans
   - Success: Each bean has single definition
   - Effort: ~1 hour
   ```

2. **Consider Feature Flag** (OPTIONAL but RECOMMENDED):
   ```
   Optional Enhancement: Add widget.enabled configuration property
   - Allows disabling widget-demo module if regression occurs
   - Provides safe rollback without code changes
   - Implementation: Spring @ConditionalOnProperty annotation
   - Effort: ~1-2 hours
   ```

3. **Enhance Rollback Validation** (LOW PRIORITY):
   ```
   Optional Subtask 1.4: Rollback dry-run
   - After baseline capture, create backup branch
   - Test rollback procedure in separate branch
   - Validates rollback steps work before migration
   - Effort: ~30 minutes
   ```

---

### Maintainability NFRs

**Status**: ✅ PASS

**Assessed Requirements**:
1. ✅ **Test coverage ≥85% line** - Validated in Subtask 12.2
2. ✅ **Mutation coverage ≥80%** - Validated in Subtask 12.3 (optional)
3. ✅ **Zero detekt violations** - Validated in Subtask 12.1
4. ✅ **Zero ktlint violations** - Validated in Subtask 12.1
5. ✅ **Architecture boundaries maintained** - Validated in Subtask 11.4 (konsistTest)
6. ✅ **Documentation updated** - Task 13 addresses all docs

**Evidence in Story**:
- Subtask 12.1: `./gradlew detekt ktlintCheck` (zero violations)
- Subtask 12.2: `./gradlew test integrationTest jacocoTestReport` (≥85% coverage)
- Subtask 12.3: `./gradlew pitest` (≥80% mutation, optional)
- Subtask 11.4: `./gradlew konsistTest` (architecture compliance)
- Task 13: Component-specifications.md and CLAUDE.md updates

**Assessment**: All maintainability requirements are explicitly addressed with concrete validation steps. Story follows Constitutional TDD philosophy with comprehensive quality gates.

**Conclusion**: ✅ PASS - Maintainability requirements fully satisfied.

### Minor Observations

**Mutation Testing Status**:
- Currently OPTIONAL (Subtask 12.3: "OPTIONAL (Time Permitting)")
- Recommendation: Consider making mandatory for refactoring stories (behavioral preservation critical)

**Static Analysis Scope**:
- Currently runs on all modules (could be optimized)
- Recommendation: Run on `products:widget-demo` only for faster feedback

### Recommendations

**NICE-TO-HAVE**:

1. **Make Mutation Testing Mandatory for Refactorings**:
   ```
   Update Subtask 12.3: Remove "OPTIONAL" designation
   - Justification: Refactoring = behavior preservation critical
   - Mutation testing best validates behavioral equivalence
   - Estimated additional time: +10-15 minutes to build
   ```

2. **Optimize Static Analysis Execution**:
   ```
   Update Subtask 12.1: Target specific modules
   - Command: ./gradlew :products:widget-demo:detekt :products:widget-demo:ktlintCheck
   - Benefit: Faster feedback cycle, focused results
   ```

3. **Add Architecture Baseline Capture**:
   ```
   NEW Subtask 1.4: Capture konsistTest baseline
   - Run: ./gradlew konsistTest > .ai/konsist-baseline.txt
   - Enables precise before/after architecture comparison
   - Detects unintended boundary violations
   ```

---

## Overall NFR Assessment

### Quality Score Calculation
```
Base Score: 100
- Security CONCERNS: -10 points
- Performance PASS: 0 points
- Reliability CONCERNS: -10 points
- Maintainability PASS: 0 points
───────────────────────────
Final Score: 80/100
```

### Gate Impact
- 2 CONCERNS categories → Gate Decision likely: **CONCERNS**
- No FAIL categories → Not a blocking issue
- Strong maintainability and performance → Can proceed with improvements

### Summary of Findings

**Strengths** ✅:
- Comprehensive test coverage validation
- Strong performance validation strategy
- Excellent maintainability safeguards
- Clear rollback documentation

**Weaknesses** ⚠️:
- Missing security scanning validation
- No automated rollback mechanism
- Bean conflict detection not explicit

**Critical Gaps** ❌:
- **NONE** - All gaps are addressable enhancements

### Recommendations Priority

**MUST ADD** (Before Implementation):
1. Secret scanning (Security gap)
2. OWASP dependency check (Security gap)

**SHOULD ADD** (Improves Quality):
3. Bean conflict detection (Reliability)
4. Make mutation testing mandatory (Maintainability)

**NICE-TO-HAVE** (Future Enhancement):
5. Feature flag support (Reliability)
6. Optimize static analysis scope (Performance)

---

## Compliance Check

### ISO 27001/ASVS Alignment
- ✅ Security controls preserved (tenant isolation, JWT validation)
- ⚠️ Secret management validation missing (add scanning)
- ✅ Audit trail maintained (Change Log in story)

### Project Quality Standards
- ✅ Test coverage requirements met (≥85% line, ≥80% mutation)
- ✅ Zero violations policy maintained (detekt, ktlint)
- ✅ Architecture compliance enforced (Konsist)
- ✅ Documentation standards followed

---

## Conclusion

**Overall NFR Status**: ⚠️ CONCERNS (Security and Reliability gaps)

**Path to PASS Status**:
1. Add secret scanning validation → Resolves Security CONCERNS
2. Add OWASP dependency check → Resolves Security CONCERNS
3. Add bean conflict detection → Resolves Reliability CONCERNS

**Estimated Effort to Achieve PASS**: 2-3 hours additional work

**Recommendation**: Implement the 3 MUST ADD/SHOULD ADD items to upgrade NFR status from CONCERNS to PASS before beginning migration implementation.