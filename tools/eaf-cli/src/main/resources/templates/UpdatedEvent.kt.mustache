package com.axians.eaf.api.{{aggregateNameKebab}}.events

import java.time.Instant
{{#fields}}
{{#kotlinType}}
import {{kotlinType}}
{{/kotlinType}}
{{/fields}}

data class {{aggregateName}}UpdatedEvent(
    val {{aggregateIdField}}: String,
    val tenantId: String,
{{#fields}}
    val {{name}}: {{type}}?,
{{/fields}}
    val updatedAt: Instant = Instant.now(),
)
