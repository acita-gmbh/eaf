import { Create, SimpleForm, TextInput, NumberInput, BooleanInput, DateTimeInput, required } from 'react-admin';
import { Typography, Box } from '@mui/material';

/**
 * {{resourceName}} Create Form
 *
 * Generated by EAF CLI (Story 7.4b)
 * Framework: @axians/eaf-admin-shell v0.1.0
 *
 * Features:
 * - Field grouping with section headings (UX enhancement)
 * - Custom validation messages
 * - Helper text for complex fields
 * - WCAG AA accessible
 */
export const {{resourceName}}Create = () => (
  <Create>
    <SimpleForm>
      <Typography variant="h6" sx={{ mt: 2, mb: 2, color: 'primary.main', borderBottom: 1, borderColor: 'divider', pb: 1 }}>
        Basic Information
      </Typography>

      {{#fields}}
      {{#isString}}
      <TextInput
        source="{{fieldName}}"
        label="{{fieldLabel}}"
        validate={required()}
        helperText="Enter {{fieldLabel}}"
        fullWidth
      />
      {{/isString}}
      {{#isNumber}}
      <NumberInput
        source="{{fieldName}}"
        label="{{fieldLabel}}"
        validate={required()}
        helperText="Numeric value for {{fieldLabel}}"
      />
      {{/isNumber}}
      {{#isBoolean}}
      <BooleanInput
        source="{{fieldName}}"
        label="{{fieldLabel}}"
        defaultValue={false}
      />
      {{/isBoolean}}
      {{#isDate}}
      <DateTimeInput
        source="{{fieldName}}"
        label="{{fieldLabel}}"
        validate={required()}
      />
      {{/isDate}}
      {{/fields}}
    </SimpleForm>
  </Create>
);
