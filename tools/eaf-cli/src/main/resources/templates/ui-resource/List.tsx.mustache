import { List, Datagrid, TextField, NumberField, BooleanField, DateField, EditButton } from 'react-admin';
import { EmptyState, LoadingSkeleton } from '@axians/eaf-admin-shell';
import type { {{resourceName}} } from './types';

/**
 * {{resourceName}} List View
 *
 * Generated by EAF CLI (Story 7.4b)
 * Framework: @axians/eaf-admin-shell v0.1.0
 *
 * Features:
 * - DataGrid with pagination
 * - Filtering by tenant (automatic via dataProvider)
 * - Empty state with CTA
 * - Loading skeleton for perceived performance
 */
export const {{resourceName}}List = () => (
  <List
    empty={<EmptyState message="No {{resourceNameLowerCase}}s yet" ctaLabel="Create First {{resourceName}}" ctaLink="/{{resourceNameLowerCase}}/create" />}
    perPage={25}
    sort={{ field: 'createdAt', order: 'DESC' }}
  >
    <Datagrid>
      <TextField source="id" label="ID" sortable={false} />
      {{#fields}}
      {{#isString}}
      <TextField source="{{fieldName}}" label="{{fieldLabel}}" />
      {{/isString}}
      {{#isNumber}}
      <NumberField source="{{fieldName}}" label="{{fieldLabel}}" />
      {{/isNumber}}
      {{#isBoolean}}
      <BooleanField source="{{fieldName}}" label="{{fieldLabel}}" />
      {{/isBoolean}}
      {{#isDate}}
      <DateField source="{{fieldName}}" label="{{fieldLabel}}" showTime />
      {{/isDate}}
      {{/fields}}
      <EditButton />
    </Datagrid>
  </List>
);
