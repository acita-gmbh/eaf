import { Show, SimpleShowLayout, TextField, NumberField, BooleanField, DateField } from 'react-admin';
import { Typography } from '@mui/material';

/**
 * {{resourceName}} Detail View
 *
 * Generated by EAF CLI (Story 7.4b)
 * Framework: @axians/eaf-admin-shell v0.1.0
 *
 * Features:
 * - Section headings for visual hierarchy
 * - Read-only field display
 * - Metadata section (created/updated timestamps)
 */
export const {{resourceName}}Show = () => (
  <Show>
    <SimpleShowLayout>
      <Typography variant="h6" sx={{ mt: 2, mb: 1, color: 'primary.main', borderBottom: 1, borderColor: 'divider', pb: 1 }}>
        Basic Information
      </Typography>

      <TextField source="id" label="ID" />

      {{#fields}}
      {{#isString}}
      <TextField source="{{fieldName}}" label="{{fieldLabel}}" />
      {{/isString}}
      {{#isNumber}}
      <NumberField source="{{fieldName}}" label="{{fieldLabel}}" />
      {{/isNumber}}
      {{#isBoolean}}
      <BooleanField source="{{fieldName}}" label="{{fieldLabel}}" />
      {{/isBoolean}}
      {{#isDate}}
      <DateField source="{{fieldName}}" label="{{fieldLabel}}" showTime />
      {{/isDate}}
      {{/fields}}

      <Typography variant="h6" sx={{ mt: 3, mb: 1, color: 'primary.main', borderBottom: 1, borderColor: 'divider', pb: 1 }}>
        Metadata
      </Typography>

      <DateField source="createdAt" label="Created At" showTime />
      <DateField source="updatedAt" label="Last Updated" showTime />
    </SimpleShowLayout>
  </Show>
);
