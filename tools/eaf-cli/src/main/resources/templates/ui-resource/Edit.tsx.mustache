import { Edit, SimpleForm, TextInput, NumberInput, BooleanInput, DateTimeInput, required, Toolbar, SaveButton, DeleteButton } from 'react-admin';
import { Typography, Box } from '@mui/material';
import { TypeToConfirmDelete } from '@axians/eaf-admin-shell';

/**
 * {{resourceName}} Edit Form
 *
 * Generated by EAF CLI (Story 7.4b)
 * Framework: @axians/eaf-admin-shell v0.1.0
 *
 * Features:
 * - Optimistic updates (mutationMode="optimistic")
 * - Type-to-confirm delete pattern (prevents accidental deletions)
 * - Undoable delete with 5-second undo
 * - Dirty form detection (warns on navigation)
 */

const EditToolbar = () => (
  <Toolbar sx={{ justifyContent: 'space-between' }}>
    <TypeToConfirmDelete resource="{{resourceNameLowerCase}}" confirmField="name" />
    <Box>
      <SaveButton />
    </Box>
  </Toolbar>
);

export const {{resourceName}}Edit = () => (
  <Edit mutationMode="optimistic">
    <SimpleForm toolbar={<EditToolbar />}>
      <Typography variant="h6" sx={{ mt: 2, mb: 2, color: 'primary.main', borderBottom: 1, borderColor: 'divider', pb: 1 }}>
        Basic Information
      </Typography>

      {{#fields}}
      {{#isString}}
      <TextInput
        source="{{fieldName}}"
        label="{{fieldLabel}}"
        validate={required()}
        fullWidth
      />
      {{/isString}}
      {{#isNumber}}
      <NumberInput
        source="{{fieldName}}"
        label="{{fieldLabel}}"
        validate={required()}
      />
      {{/isNumber}}
      {{#isBoolean}}
      <BooleanInput
        source="{{fieldName}}"
        label="{{fieldLabel}}"
      />
      {{/isBoolean}}
      {{#isDate}}
      <DateTimeInput
        source="{{fieldName}}"
        label="{{fieldLabel}}"
        validate={required()}
      />
      {{/isDate}}
      {{/fields}}
    </SimpleForm>
  </Edit>
);
