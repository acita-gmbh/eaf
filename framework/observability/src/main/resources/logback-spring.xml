<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- JSON Encoder for structured logging -->
    <springProfile name="!test">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <!-- Standard fields -->
                    <timestamp>
                        <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</pattern>
                    </timestamp>
                    <logLevel/>
                    <loggerName/>
                    <message/>
                    <threadName/>

                    <!-- Context fields from MDC -->
                    <mdc>
                        <includeKeyName>service_name</includeKeyName>
                        <includeKeyName>trace_id</includeKeyName>
                        <includeKeyName>tenant_id</includeKeyName>
                    </mdc>

                    <!-- Exception details -->
                    <stackTrace/>
                </providers>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>

    <!-- Plain text logging for tests to avoid JSON parsing complexity in test output -->
    <springProfile name="test">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>